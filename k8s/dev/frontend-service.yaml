apiVersion: v1
kind: Service
metadata:
  name: ai-codegen-frontend-service
  namespace: development
  labels:
    app: ai-codegen-frontend
spec:
  type: ClusterIP
  selector:
    app: ai-codegen-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
